import styled from "styled-components";

export default styled.p`
  font-size: ${14 / 16}rem; // 12px
  font-family: var(--font-family-secondary);
  /* color: var(--color-text-tint); */
  line-height: 1;
`;
