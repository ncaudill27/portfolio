import styled from "styled-components";

export default styled.code`
  background-color: var(--color-text-transparent-3);
  font-family: var(--font-family-code);
  font-size: 0.9em;
  padding: 2px var(--spacing-0);
  border-radius: 2px;
  white-space: nowrap;
`;
